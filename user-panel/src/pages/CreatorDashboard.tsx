/**
 * Creator / author analytics dashboard (blog performance, growth charts, posts, program & earnings).
 * Implementation lives in `creator-dashboard/` as smaller modules.
 */
export { default } from './creator-dashboard/CreatorDashboardPage'
